# Secrets referenced from config/config.yaml as ${VAR}
SIP_PASSWORD_491=change-me
SIP_PASSWORD_492=change-me

# Only needed when api.enabled is true; at least 16 characters
# SIPGRAM_API_TOKEN=change-me-to-something-long

# Timezone of the container: the schedule section (work_hours / quiet_hours) uses local time
TZ=Europe/Moscow
